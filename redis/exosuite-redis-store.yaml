apiVersion: v1
kind: Service
metadata:
  name: exosuite-redis-store
  labels:
    app: exosuite-redis-store
spec:
  type: ClusterIP
  ports:
    - name: redis
      protocol: TCP
      port: 6379
  selector:
    run: exosuite-redis-store
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: exosuite-redis-store
spec:
  selector:
    matchLabels:
      run: exosuite-redis-store
  replicas: 1
  template:
    metadata:
      labels:
        run: exosuite-redis-store
    spec:
      volumes:
        - name: redis-store-storage
          persistentVolumeClaim:
            claimName: exosuite-redis-store-claim
      containers:
        - name: exosuite-redis-store
          image: redis:alpine
          ports:
            - containerPort: 6379
          command: ["sh"]
          args: ["-c", "docker-entrypoint.sh --appendonly yes"]
          volumeMounts:
            - mountPath: "/data"
              name: redis-store-storage
---
kind: PersistentVolume
apiVersion: v1
metadata:
  name: exosuite-redis-store-volume
  labels:
    type: local
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  storageClassName: manual
  hostPath:
    path: "/var/www/exosuite-redis-store"
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: exosuite-redis-store-claim
spec:
  resources:
    requests:
      storage: 10Gi
  storageClassName: manual
  accessModes:
    - ReadWriteMany